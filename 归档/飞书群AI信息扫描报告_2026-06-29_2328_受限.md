# 飞书群 AI 信息扫描报告（受限版）

> **扫描时间**：2026-06-29 23:28  
> **目标群聊**：Hermes 中文社区 1（`oc_5335652b0dca214b837cfcd1456061db`）  
> **时间窗口**：2026-06-28 00:00 ~ 2026-06-29 23:28（约 47 小时）  
> **状态**：❌ 未能获取消息数据

---

## 一、消息统计

| 指标 | 结果 |
|------|------|
| 总消息数 | **无法统计**（权限不足） |
| 活跃成员 TOP5 | 无法统计 |
| 重点内容数 | 0 条 |
| 链接数 | 0 条 |

**说明**：从 2026-06-29 09:33 之后，已连续 6 次扫描无法拉到该群新数据。

---

## 二、失败原因

尝试使用命令：

```bash
lark-cli im +chat-messages-list \
  --chat-id oc_5335652b0dca214b837cfcd1456061db \
  --start "2026-06-28T00:00:00+08:00" \
  --end "2026-06-29T23:28:00+08:00" \
  --page-size 50 --order asc --as user
```

返回错误：

```json
{
  "ok": false,
  "error": {
    "type": "authorization",
    "subtype": "missing_scope",
    "message": "missing required scope(s): im:message.group_msg:get_as_user, im:message.p2p_msg:get_as_user, im:message.reactions:read, contact:user.base:readonly",
    "identity": "user"
  }
}
```

同时尝试搜索二哥（灵枢）的 open_id 也失败：

```bash
lark-cli contact +search-user --query "灵枢" --as user
```

返回错误：

```json
{
  "ok": false,
  "error": {
    "type": "authorization",
    "subtype": "missing_scope",
    "message": "missing required scope(s): contact:user:search"
  }
}
```

### 根因总结

1. **User 身份缺少 read 类 scope**：`im:message.group_msg:get_as_user`、`im:message.p2p_msg:get_as_user`、`im:message.reactions:read`、`contact:user.base:readonly`、`im:chat:read`、`contact:user:search` 等均未授权。
2. **Bot 不在目标群内**：`230002 Bot can NOT be out of the chat`，无法以 bot 身份读取消息。
3. **二哥 open_id 失效**：记忆中旧 open_id `ou_c0add71d24478b50f3e6b6b7ac8b08af` 提示 `open_id cross app`，无法直接用于私聊推送；且因缺少搜索 scope，无法重新查找正确的 open_id。
4. **App 后台已开通权限，但 User 授权时未勾选**：历史记录显示 app 后台 227 个 scope 中包含 read 类，但当前登录的 user 授权范围只覆盖了 send 类。

---

## 三、修复路径

请二哥在本机运行以下命令重新授权（建议复制到终端执行）：

```bash
lark-cli auth login --scope "im:message.group_msg:get_as_user im:message.p2p_msg:get_as_user im:message.reactions:read contact:user.base:readonly search:message contact:user.basic_profile:readonly contact:user:search im:chat:read"
```

执行后会输出一个验证 URL，用浏览器打开并完成飞书授权即可。

授权成功后，建议再做一次：

```bash
lark-cli contact +search-user --query "灵枢" --as user
```

拿到正确的 `open_id` 后更新记忆，私聊推送即可恢复。

---

## 四、建议

1. **如果短期内不修复授权**：建议将此自动化状态设置为 **PAUSED**，避免每 2 小时空跑消耗资源。
2. **如果修复授权**：保持 **ACTIVE**，下一次扫描即可正常拉取近两日群消息并生成完整报告。
3. **关于本次时间窗口**：自 06-29 09:33 之后约 14 小时未覆盖到数据，按历史活跃规律，晚间时段可能有不少 AI 工具、模型、链路相关讨论，建议修复后补扫。

---

## 五、对二哥的价值

- **影视剪辑/短视频策划**：群内常分享 AI 视频生成、配音、模型性价比信息（如 DeepSeek、MiniMax、Hermes 等），修复后可补回这些素材。
- **电商运营/变现**：CPS、AI 工具、知识付费相关话题偶有讨论，可从中提取风向标。
- **当前状态**：本次无可用信息，建议优先修复授权，避免持续信息断档。

---

*报告由小二娃自动生成*  
*私聊推送状态：失败（open_id 跨应用/搜索权限缺失）*
