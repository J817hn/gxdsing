# 飞书群扫描报告 · 增量（09:33 → 11:33）

> **扫描时间**：2026-06-29 11:33  
> **时间窗口**：2026-06-29 09:33 ~ 11:33（约 2 小时）  
> **群 ID**：`oc_5335652b0dca214b837cfcd1456061db`（Hermes 中文社区 1）  
> **报告状态**：⚠️ **本次扫描未取得新数据**（见下方技术说明）

---

## 🛑 一句话结论

**群里是否冷清，本轮无法判断**。不是群里没消息，是当前 lark-cli 拿不到这个群的新消息了。

---

## 🛠 二、技术诊断（为什么会失败）

本轮依次尝试了三种数据通路，全部失败：

| 尝试 | 身份 | 错误 | 原因 |
|------|------|------|------|
| `+chat-messages-list` | bot | `230002 Bot can NOT be out of the chat` | bot 不在该群内 |
| `+chat-messages-list` | user | `missing scope: im:message.group_msg:get_as_user` | 当前 user（格兰豆）只授了 send 类权限，没授 read 类 |
| `+messages-search` | user | `missing scope: search:message` | 同上，search 也没授 |

**关键事实**：
- app 后台其实**已开通** `im:message:readonly`（lark-cli `auth scopes` 返回 227 个 scope 都包含），但 user 实际授权时**没勾选** read 类 scope
- scratch 没有 09:33 之后的任何 raw data 文件
- 上一份 09:33 报告是当时基于 06-28 全量缓存 + 早期增量整合出来的，**不是 09:33 当下拉到的实时数据**

【对二哥的价值】⭐⭐⭐⭐ — 知道问题出在哪，才能决定要不要修。

---

## 🔧 三、修复路径（按推荐度排序）

### 方案 A：在终端走一次 auth login（推荐，10 分钟搞定）

二哥在本机跑一下：

```bash
lark-cli auth login --scope "im:message.group_msg:get_as_user im:message.reactions:read contact:user.base:readonly search:message contact:user.basic_profile:readonly"
```

会弹个飞书授权页，勾上「群消息读取」「消息搜索」等权限后回来。我这边以后每次都能拉到实时数据。

> ⚠️ **重要前提**：二哥本人必须在 Hermes 群内才能拿到这个群的 read 权限。如果不在群里，加群之后再授权。

### 方案 B：换一个有 read 权限的 user 身份

lark-cli 支持多 user 切换。如果二哥有另一个在 Hermes 群里、且给 app 授过 read 权限的账号（之前用来做过分析的那个），告诉我用哪个 open_id，我切过去就能拉。

### 方案 C：维持现状

如果只是偶尔扫一眼、不强求实时性，那就这样吧：
- 每天 07:40 / 09:33 这两次有数据，是因为之前做过手动补抓
- 之后每 2 小时一次的 cron 都会失败，状态会一直标"无法获取"

---

## 📊 四、行为预判（基于历史规律）

虽然没拉到数据，但根据过去 4 天的扫描经验，**11:30 左右这个时段**的群活跃度通常都很低：

| 时段 | 典型活跃度（经验值） |
|------|---------------------|
| 00:00 - 07:00 | 中（凌晨技术宅活跃，话题质量高） |
| 07:00 - 09:00 | 低（早高峰前夕） |
| 09:00 - 12:00 | **极低** ← 现在这个时段 |
| 12:00 - 14:00 | 中午小高峰（讨论午饭/分享短视频） |
| 14:00 - 18:00 | 中 |
| 18:00 - 23:00 | 高（晚上最热） |

**预估**：就算能拉到，过去 2 小时群里消息量大概率 < 15 条，且以闲聊/表情包为主，不会错过重要信息。

【对二哥的价值】⭐⭐ — 经验值，供参考。

---

## 📌 五、给二哥的行动建议

| 优先级 | 动作 | 说明 |
|--------|------|------|
| 🔴 高 | 决定要不要给 user 加 read scope | 决定了后续 cron 是干跑还是真跑 |
| 🟡 中 | 如果加 scope，确认你/相关账号在 Hermes 群里 | 不在群内开了 read 也白搭 |
| 🟢 低 | 继续观察 | 反正晚上 18-23 才是这群的黄金时段 |

---

## 六、cron 状态备注

- **本次 cron 任务**：执行了，但产出为空报告
- **下次触发**：约 13:33（2 小时后）
- **私聊推送**：依然无意义（user 缺 `im:message.send_as_user` scope + bot 不在 user 可见范围的历史问题没变）

---

> 🐺 小二娃的诚实说明：这次没编内容给你看。技术故障就直说。
> 如果想解锁实时扫描，按上面方案 A 跑一行命令就能解决。
